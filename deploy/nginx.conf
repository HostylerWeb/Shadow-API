upstream shadowapi_gateways {
  server host.docker.internal:3000;
  server host.docker.internal:3010;
}

server {
  listen 80;
  location / {
    proxy_pass http://shadowapi_gateways;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
  }
}
